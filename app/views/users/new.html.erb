<% if user_signed_in? %>
  <h1>New user</h1>
  
  <%= render 'form' %>
  
  <%= link_to 'Back', users_path %>
<% else %>

  <h1>You must be an admin to do this!</h1>

<% end %>
<%= link_to 'Back', votes_path %>